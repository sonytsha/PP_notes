// rules for evaluating an expression
// a+b*c/d-e%f+g
// 5+2*3 = 5+6=11
// 7*3=21
// BODMAS : convention, operator precedence
// *********** ( post ++ , -- ) > (pre ++, --) > ( *,/,%) [ L->R] > (+,-) [ L-> R] *************************************
// if we have operators of equal precedence , how to evaluate  ? == > associativity rule
// L->R ( left tpo right) , R -> L

#include<stdio.h>
int main() {
int a,b,c;
scanf("%d %d %d" , &a, &b, &c);
int z= a * b / c;
printf("%d", z);
return 0;
}


#include<stdio.h>
int main() { 
int x = 5 + 9 % 4* 3 - 2 + 6 / 4;
printf("%d \n", x);
return 0 ;
}

// 5+1*3-2+6/4
// 5 + 3 - 2 + 6 / 4
// 5 + 3 - 2 + 1
// 7
